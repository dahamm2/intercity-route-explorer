{
  "name": "intercity-route-explorer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare-data": "node scripts/prepare-data.mjs",
    "dev": "npm run prepare-data && vite",
    "build": "npm run prepare-data && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
